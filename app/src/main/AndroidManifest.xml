<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.questionhat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TrackActivity" />
        <activity android:name=".ChoosePath" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SuccessActivity" />
        <activity android:name=".Forget_Password_UE" />
        <activity android:name=".Verify_email_otb" />
        <activity android:name=".CoinsActivity" />
        <activity android:name=".LeaderBoard" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".New_Email_Password"></activity>
    </application>

</manifest>